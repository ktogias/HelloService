# HelloService

Provides a json api to get the "hello" string

## How to build:

podman build -t hello .

## How to run:

podman run --publish 8080:80 hello

## View in browser:

http://localhost:8080